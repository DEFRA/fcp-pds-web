services:
  fcp-pds-web:
    build:
      context: .
      target: production
    image: fcp-pds-web
    container_name: fcp-pds-web
    environment:
      APPINSIGHTS_CLOUDROLE: fcp-pds-web-local
      APPLICATIONINSIGHTS_CONNECTION_STRING: ${APPLICATIONINSIGHTS_CONNECTION_STRING}
